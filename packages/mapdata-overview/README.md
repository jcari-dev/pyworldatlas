# PyWorldAtlas Overview map data

This companion package contains compact global terrain, climate, outline, and
river data for the optional PyWorldAtlas browser maps. Install it through
`pyworldatlas[maps-overview]` rather than installing it directly.
